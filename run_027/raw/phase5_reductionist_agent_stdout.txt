{
  "agent": "REDUCTIONIST",
  "results": [
    {
      "reframe_id": "A1",
      "surface_phenomenon": "After cyclic/periodic training that returns the network to the same effective function and loss geometry, the weights do not return to themselves: the optimal neuron-alignment permutation pi_k between cycle-start and post-cycle-k weights is a reproducible (across seeds) non-identity element of S_n that accumulates additively/ballistically in k, while the loss curve shows no corresponding feature.",
      "reduction_chain": [
        "Level 0 (observable): reproducible non-identity Hungarian-matched permutation pi_k after k closed-loop training cycles, with no loss signature.",
        "Level 1 (geometry of parameter space): the true object of optimization is not raw weight space R^d but the quotient orbifold M = R^d / S_n, since relabeling hidden units is an exact gauge symmetry of the function; a closed loop in function space (M) lifts to an open path in the covering R^d ending at a permuted copy — the run's invariant is its monodromy/holonomy element of S_n, not its endpoint.",
        "Level 2 (where the topology lives): the quotient has non-trivial topology only because the S_n action has fixed points — the branch/orbifold locus where two or more neurons become functionally identical (degenerate). The fundamental group / deck-transformation winding is generated by loops that encircle this branch locus (the 'Brea permutation points' where neuron-swap symmetry is unbroken).",
        "Level 3 (dynamics on the orbifold): SGD is not a free path — it is integral flow of a gradient (drift) field plus noise on M. Holonomy reproducibility is a property of this field, not of topology alone: an idle loop far from the branch locus winds nothing; a loop must pass near a near-degenerate neuron pair to acquire a permutation.",
        "Level 4 (deepest mechanism — what FORCES definiteness): near a near-degenerate pair the symmetric/antisymmetric (swap-even/swap-odd) combination of the two neurons is a soft mode whose Hessian eigenvalue passes near zero; the gradient field has a NON-ZERO CURL component in this 2-neuron plane set by the data/curriculum's broken symmetry (the periodic protocol supplies a definite chirality). A non-zero circulation of the drift field around the branch locus is a topological charge (a winding number / first Chern-like index of the gradient flow) that is QUANTIZED and sign-definite. Reproducibility follows because the drift's circulation, not the diffusion, dominates the encircling: signal/noise of the angular advance per cycle is O(1) and same-signed across seeds, so each cycle advances the phase by a definite fraction of 2*pi and swaps the pair after an integer number of cycles."
      ],
      "deepest_principle": "Quantized holonomy of SGD on the orbifold R^d/S_n: a non-zero, sign-definite circulation (curl) of the gradient-drift field around the neuron-degeneracy branch locus produces a definite winding number per cycle; the diffusive part only adds zero-mean jitter. The net cyclic permutation is the integer monodromy charge of that drift circulation, which is why it is reproducible, additive in k, and noise-robust rather than diffusive.",
      "decisive_experiment": "Train a small MLP (e.g. width n=128 on a fixed task) under a strictly periodic curriculum for k=1..N cycles (N>=20), across S>=10 seeds. After each cycle, compute pi_k by Hungarian assignment on hidden-unit activation-correlation (and independently on weight matching) relative to cycle-0 weights. Track three quantities: (a) d_k = distance-from-identity of pi_k (number of displaced neurons / transpositions); (b) the per-cycle angular advance theta of each near-degenerate neuron pair in its swap-plane, estimated from the local gradient field, and its circulation oint grad·dl around the degeneracy; (c) cross-seed sign agreement of theta. Decisive test = does d_k grow additively/quantized with a reproducible direction (signal) or diffusively with zero mean (noise)?",
      "confirm_outcome": "d_k grows step-wise/ballistic and ~linear in k with mean displacement >> sqrt(k) noise floor (drift-to-diffusion ratio |E[d_k]|/std[d_k] >> 1, e.g. >3); the per-cycle increment is integer-quantized (clusters near fixed numbers of transpositions); the swap-plane circulation oint grad·dl is non-zero and SAME SIGN across >=80-90% of seeds (binomial p<<0.01 vs 50/50); and all of this occurs with the loss/training curve flat (no per-cycle feature above noise). Equivalent monodromy from activation-Hungarian and weight-matching agree.",
      "refute_outcome": "pi_k stays identity (d_k ≈ 0) for all k, OR d_k grows diffusively: E[d_k] ≈ 0 with var(d_k) ∝ k (random walk), cross-seed circulation sign is ~50/50 (no reproducible chirality, binomial p>0.05), and the per-cycle increment shows a continuous/Gaussian spread with no integer quantization. Any of these refutes the topological-charge claim and supports the standard static-redundancy/seed-noise view.",
      "existing_tool": "Hungarian/linear-sum assignment (scipy.optimize.linear_sum_assignment) on hidden-unit activation correlation matrices for activation-based alignment, plus Git Re-Basin / weight-matching code (Ainsworth et al.) for weight-based permutation; standard PyTorch/JAX training loop with a scripted periodic curriculum; Hessian-vector products (e.g. PyHessian) to identify near-degenerate neuron pairs and estimate local soft-mode eigenvalues and gradient circulation. All run on a single GPU today."
    }
  ]
}
