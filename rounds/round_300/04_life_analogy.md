# R300 — life analogy

## Source: Australian magpie individual carol signature
- Per-individual + per-group unique syllable patterns embedded in carolling repertoire.
- Other magpies can identify singer by carol — vocal fingerprint without explicit ID tag.

## LLM analogy
**MAGPIE-LLM**: per-instance generation-style fingerprint embedded statistically in output token distribution (subtle frequency biases across function words + structural patterns). Provenance verifiable by holding a small reference sample of model's outputs.

## Differs from prior art (claim)
LLM fingerprinting / watermarking literature already does this: 2505.16723 (semantically conditioned watermark), 2507.03014 (intrinsic attention-parameter fingerprint), 2509.25448 (prompt-injection fingerprint). MAGPIE-LLM = LLM fingerprinting — direct collision.
