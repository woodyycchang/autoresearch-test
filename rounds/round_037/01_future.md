By 2031, long-running agents will accumulate dubious context (poisoned retrievals, leaked traces, contaminated tool outputs). A naive sanitization erases too much; ignoring taint propagates failure. A BFP-style mechanism — context can be cleansed via a duty-of-inquiry inspection — gives a third option: structured purification with audit trail. The legal-mechanism import is the asymmetric chain-breaking property.