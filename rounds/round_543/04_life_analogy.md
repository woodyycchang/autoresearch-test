# Life Analogy — Yemeni mountain-side gravity-cascade cistern-fed terrace cascade with per-tree directional valve

The **Yemeni terraced agriculture** (Sana'a region):
- Rain-fed cisterns at top of mountain; gravity flows water downward across terraces.
- Per-tree water directional valve in dry winter (cistern → specific tree roots).
- 500+ year tradition; UNESCO Sana'a World Heritage; Al-Sanani coffee variety.
- Tree-canopy shade + altitude-gradient (2200m) microclimate.
- Cistern-cascade design retains water against semi-arid runoff.

**YEMENI-TERRACE-GRAVITY-CASCADE-CISTERN-PER-TREE-VALVE-INFOFLOW**: gravity-cascade information flow with rain-fed top-tier cistern + per-tier capacity throttle + per-token directional valve + altitude-gradient layer temperature + retention against runoff loss. (1) **Top-tier rain-fed cistern T_top**: input embedding accumulates "rainfall" (raw input information) in a capacity-constrained top-tier reservoir. (2) **Gravity downflow through K-tier cascade**: information flows down K stacked terraces; each tier has capacity throttle T_k constraining downflow rate; lower tiers receive only what upper tiers passed. (3) **Per-tree directional valve V_token**: in dry-winter regime (low-context), router decides per-token where downflow goes (specific downstream attention head/cluster); in wet-summer (high-context), default broadcast. (4) **Altitude-gradient layer temperature τ_l = T_max - α·l**: layer-l temperature decreases with depth (mountain altitude gradient) controlling sampling spread. (5) **Retention-against-runoff regularizer L_retain**: penalty on information loss past a layer; forces information to be retained per-tier. (6) Differs from R413 + R422 + R429 + R438 + R455 + R480 + R493 + R505 KASHAN-QANAT-CASCADE-INFOFLOW (multi-stage cistern context-buffer + wind-tower thermal-attention damping + vent-shaft per-stage diagnostic + sarooj impermeable boundary) and R530 KHIPU-PRIMARY-PENDANT-SUBSIDIARY-CASCADE-DECIMAL (4-tier khipu primary-pendant + 3-knot classification + decimal scaling + subsidiary correction + cord-attribute metadata) by gravity-cascade top-cistern + per-tier capacity throttle + per-token directional valve dry/wet regime + altitude-gradient layer temperature + retention-vs-runoff regularizer.

## Adjacency
- Dynamic Routing Cascading Survey 2603.04445
- Unified Routing Cascading LLMs
- C3PO LLM Cascades 2511.07396
- Cascaded LM 2506.11887

Expected FAIL — multi-tier cascade-routing + per-tier capacity + cost-constrained literature covers.
