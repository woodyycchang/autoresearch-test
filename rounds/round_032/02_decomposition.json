{"sub_problems":["Does effective context utilization decay exponentially with token distance, and if so, what is the decay constant?","Is the decay constant tau a fixed fraction of nominal context length across architectures and scales?","Can this scaling law be used to derive optimal chunk sizes for RAG without empirical sweeps?"]}
