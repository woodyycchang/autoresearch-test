# Round 680 — Future LLM/AI mechanism

E28 R680, program_v9.md. Timestamp 2026-05-20T04:46:00Z.

Use Lindenmayer L-system production rules to generate LLM
chain-of-thought structure: define an alphabet of reasoning-step
symbols, production rules that expand each symbol into sub-steps, and a
fixed recursion depth d. The LLM's CoT is constrained to follow the
L-system unfolding (one symbol → expansion → terminals).

Self-similarity from L-system recursion enforces hierarchical reasoning
structure with fractal-like step decomposition.

Form: information-cascade.
