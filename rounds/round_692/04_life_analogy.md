# R692 motivation

VC dimension = max set size that can be shattered by the hypothesis
class. Determines generalization bound. LoRA rank = adapter capacity.
Set rank via VC-style bound: capacity matched to data. Motivation:
mechanism_transfer.
