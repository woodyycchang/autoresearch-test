# Life Analogy — Persian qanat + ab-anbar cascade water-supply

The **qanat** + **ab-anbar** (Persian water-supply system):
- Underground horizontal aqueduct tunnel collects water from alluvial aquifer via "mother well".
- Vertical vent/spoil shafts at regular intervals along tunnel (km-long).
- Gravity-driven flow throughout, no pumping — sustainable + zero-energy.
- Downstream **jubs** (surface canals) + **kariz** (urban distribution sub-network).
- **Ab-anbar** (reservoir cistern, below ground, sarooj-mortar walls) collects water at intervals.
- **Wind tower** atop ab-anbar: pressure differential pulls air from qanat tunnel, cooling water + ventilating cistern (25°C summer water in 45°C desert).

**KASHAN-QANAT-CASCADE-INFOFLOW**: information-cascade with multi-stage chamber buffers + vent-shaft sampling + wind-tower cooling. (1) **Mother well = input layer** at full information density (raw token stream). (2) **Underground tunnel = stage-1 hidden axis** with gravity-driven information flow (no compute, pure routing — like residual stream). (3) **Regular vent shafts = per-stage sampling/inspection ports** allowing diagnostic-readout + per-shaft layer-norm injection without disrupting main flow. (4) **Multiple sequential ab-anbar cisterns = K stacked context-buffer chambers** at intervals along stream, each cistern aggregates information accumulated since last cistern + provides downstream buffer. (5) **Wind-tower cooling = thermal-attention damping** — at each cistern, cross-attention with a "cool" reference embedding (long-term-anchor) reduces local variance / "temperature" of activations. (6) **Sarooj-mortar = impermeable layer-context boundary** (no leakage between cisterns except through controlled outlets). (7) Differs from R413 CHASQUI (relay-runner discrete pass-through, no cistern buffer) + R422 + R429 KAMI-SHIBAI (story-cascade, no thermal cooling) + R438 PIROGUE (boat cascade) + R455 HAUSA-CASCADE + R480 RINGDANS (synchronous chorus) + R493 EISTEDDFOD by cistern-chamber buffer-aggregation + wind-tower thermal damping + vent-shaft diagnostic-readout combination.

## Adjacency
- Cascade Routing OpenReview / Unified Routing-Cascading
- HERMES Multi-Stage Pipeline (closest)
- Self-Aware Token-Efficient EMNLP 2025
- StreamingLLM attention sink / residual stream
- Layer-Norm + attention damping literature

Expected FAIL — multi-stage cascade routing + multi-buffer pipeline + attention damping fully covered.
