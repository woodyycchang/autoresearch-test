# Round 691 — E28 R691, v9. Timestamp 2026-05-20T05:30:00Z.

Apply Sobolev embedding theorem (W^{k,p} ⊂ C^m if k - n/p > m) to LLM
loss: define a graded Sobolev norm over weight perturbations and use the
embedding to derive a continuous lower bound on basin stability. Stronger
embedding → wider basin.

Form: basin-stability.
